name: DevOps CI/CD

on:
  push:
    branches:
      - main:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v3

    - name: Build Docker imaage
      run: docker buid -t myapp .

